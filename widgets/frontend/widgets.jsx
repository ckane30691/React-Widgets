import React from "react";
import ReactDOM from "react-dom";
import WidgetContainer from './widgetContainer';

document.addEventListener('DOMContentLoaded', ()=>{
  ReactDOM.render(<WidgetContainer />, document.getElementById('root'));
});
